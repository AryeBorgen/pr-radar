node_modules
dist
*.local
.DS_Store
*.tsbuildinfo

# Playwright
test-results
playwright-report
blob-report

# Build output. dist-lib and dist-server are published from a fresh build,
# never from the tree.
dist-lib
dist-server
*.tgz

worker/.wrangler
